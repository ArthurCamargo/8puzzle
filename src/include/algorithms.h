#include <stdio.h>
#include <string>

#include "node.h"
#include "state.h"


class Algorithm {
    // Algorithms that can be used
    private:
        std::string desc; // Description of the algorithm
    public:;
};
